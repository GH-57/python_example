# 1. Django의 MVT 패턴에서 'T'가 의미하는 것은?
# - Template

# 2. Django models.py에서 auto_now_add=True 옵션의 의미는?
# - 레코드가 처음 생성될 때만 현재 시간으로 설정

# 3. Django Form에서 forms.ModelForm을 사용하는 주요 이유는?
# - 모델과 연동하여 폼 필드를 자동구성하기 위해

# 4. Django URL 패턴에서 <int:id> 의 의미는?
# - 정수형 값을 id 변수로 캡처

# 5. Django에서 GET과 POST 요청을 구분하는 방법은?
# - request.method

# 6. Django에서 redirect() 함수의 목적은?
# - HTTP 리다이렉트 응답 생성

# 7. Django 설정에서 MEDIA_URL과 MEDIA_ROOT의 관계는?
# - MEDIA_URL은 웹 접근 URL, MEDIA_ROOT는 파일 저장 경로

# 8. Django Form에서 form.is_valid() 메서드의 역할은?
# - 폼 필드 구성 확인 (X)
# - 사용자 입력 데이터 유효성 검사 (O)

